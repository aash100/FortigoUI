import { FortigoSubmitButtonDirective } from './fortigo-submit-button.directive';

describe('FortigoSubmitButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new FortigoSubmitButtonDirective();
    expect(directive).toBeTruthy();
  });
});
