import { FortigoNavigationButtonDirective } from './fortigo-navigation-button.directive';

describe('FortigoNavigationButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new FortigoNavigationButtonDirective();
    expect(directive).toBeTruthy();
  });
});
