import { FortigoClearButtonDirective } from './fortigo-clear-button.directive';

describe('FortigoClearButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new FortigoClearButtonDirective();
    expect(directive).toBeTruthy();
  });
});
