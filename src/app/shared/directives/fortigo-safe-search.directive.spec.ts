import { FortigoInputValidatorDirective } from './fortigo-safe-search.directive';

describe('FortigoSafeSearchDirective', () => {
  it('should create an instance', () => {
    const directive = new FortigoInputValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
